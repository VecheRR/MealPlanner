# 🍽️ AI Meal Planner

AI Meal Planner — это iOS-приложение для автоматического планирования питания по целям пользователя (похудение, поддержание веса или набор массы).

Приложение сочетает **умную генерацию плана питания**, **расчёт калорий** и **получение реальных рецептов через API**.

---

## ▶️ Как запустить проект

### 1. Клонировать репозиторий
```bash
git clone https://github.com/VecheRR/MealPlanner.git
cd MealPlanner
```

### 2. Открыть проект в Xcode
Открой файл:
```text
MealPlanner.xcodeproj
```

### 3. Создать конфигурационный файл с ключами
Ключи **не хранятся в репозитории** и передаются через xcconfig.
В корне проекта создай файл **Config.xcconfig**:
```xcconfig
OPENAI_API_KEY = sk-ВАШ_API_KEY
```

### 4. Подключить Config.xcconfig в Xcode
	1.	Открой Project → Info
	2.	В разделе Configurations:
	•	Debug → Config.xcconfig
	•	Release → Config.xcconfig
	3.	В Info.plist должен быть ключ:
  ```text
  OPENAI_API_KEY = $(OPENAI_API_KEY)
  ```
### 5. Запустить приложение
	1.	Выбери target MealPlanner
	2.	Выбери симулятор iPhone
	3.	Нажми Run (▶︎)

---

## 📱 Основные возможности

- Выбор цели питания: похудение / поддержание / набор
- Расчёт индивидуальной нормы калорий по параметрам пользователя  
  (пол, возраст, рост, вес, уровень активности)
- Генерация плана питания на несколько дней
- Отображение калорий и макросов (белки, жиры, углеводы)
- Поиск реальных рецептов для каждого блюда
- Сохранение сгенерированных планов в истории
- Отображение прогресса генерации в интерфейсе

---

## 🤖 AI-часть

Для генерации плана питания используется **внешний AI-сервис OpenAI**, подключённый через REST API.

- AI работает через облачный API
- Используется для генерации структуры плана питания и блюд
- Возвращает данные в структурированном формате JSON
- Интеграция реализована через `URLSession`
- API-ключ передаётся через `xcconfig` и не хранится в репозитории

AI отвечает за интеллектуальную генерацию плана питания на основе параметров пользователя (цель, калории, ограничения).

---

## 🌐 API

Для получения реальных рецептов используется внешний API:

**TheMealDB**
- Открытый REST API
- Используется для поиска рецептов по ключевым словам
- Возвращает реальные блюда, ингредиенты и инструкции

AI отвечает за план питания, а API — за реальные данные о еде.

---

## 🧮 Расчёт калорий

Перед генерацией плана приложение рассчитывает норму калорий пользователя по формуле **Миффлина — Сан Жеора**.

На основе расчёта автоматически определяются:
- калории для похудения
- калории для поддержания веса
- калории для набора массы

Выбранное значение используется при генерации плана питания.

---

## 🏗️ Архитектура

Проект разделён на логические слои:

- **Screens** — UI экраны (Setup, Plan, History, Recipes)
- **ViewModel** — бизнес-логика и состояние приложения
- **Services** — AI, API и хранение данных
- **Core** — модели, расчёты и вспомогательные структуры

Архитектура основана на SwiftUI и MVVM.

---

## 🛠️ Технологии

- Swift / SwiftUI
- MVVM
- OpenAI API
- URLSession
- TheMealDB API
- JSON
- xcconfig для хранения секретов
- Локальное хранение данных (FileManager)

---

## ⚠️ Примечания

- Для работы AI требуется действующий API-ключ OpenAI
- Приложение является рабочим прототипом и учебным проектом
- Интерфейс ориентирован на функциональность и наглядность, а не на коммерческий дизайн

---

## 🎯 Назначение проекта

Проект выполнен в рамках учебного задания и демонстрирует:
- работу с API
- интеграцию AI
- расчёт бизнес-логики
- корректную архитектуру iOS-приложения

---

## 📊 План интеграции аналитики и рекламы (Этап C)

Данный этап посвящён проектированию и интеграции аналитики, атрибуции и рекламной монетизации в приложении **AI Meal Planner**.

---

## 🎯 Цели аналитики

Аналитика используется для:
- анализа поведения пользователей,
- оценки эффективности AI-генерации плана питания,
- построения пользовательской воронки,
- подготовки к рекламной монетизации и подпискам.

---

## 📌 Используемые сервисы

- **Firebase Analytics** — основная продуктовая аналитика
- **AppsFlyer** — атрибуция установок и источников трафика
- **AppMetrica** — дополнительная аналитика пользовательского поведения
- **AppHud** — аналитика подписок и монетизации
- **AdMob / ironSource** — рекламные сети (планируемая интеграция)

---

## 🧩 Пользовательские события (Events)

| Событие | Описание |
|------|---------|
| `app_open` | Запуск приложения |
| `first_launch` | Первый запуск приложения |
| `plan_generation_started` | Начало генерации плана питания |
| `plan_generation_success` | Успешная генерация плана |
| `plan_generation_failed` | Ошибка генерации |
| `recipe_opened` | Просмотр рецепта |
| `history_opened` | Просмотр истории планов |
| `paywall_opened` | Открытие экрана подписки |
| `subscription_started` | Начало подписки |
| `subscription_active` | Подписка активна |

---

## 📈 Отслеживаемые метрики

- DAU / MAU
- Conversion to Plan Generation
- AI Success Rate
- Retention (Day 1 / Day 7)
- Conversion to Subscription

---

## 🧭 Пользовательская воронка (Funnel)

1. Установка приложения  
2. Первый запуск  
3. Заполнение параметров питания  
4. Генерация плана питания  
5. Просмотр рецептов  
6. Переход к подписке  

---

## 📲 Атрибуция пользователей

Для определения источников трафика используется **AppsFlyer**:
- органические установки
- рекламные кампании
- последующая интеграция deep links

AppsFlyer используется как основной инструмент атрибуции и передачи данных в рекламные и монетизационные системы.

---

## 🔐 Privacy и App Tracking Transparency

- При первом запуске приложения запрашивается разрешение **App Tracking Transparency**
- Статус ATT учитывается при инициализации AppsFlyer и рекламных SDK
- Все данные собираются в соответствии с требованиями Apple

---

## 💰 План рекламной монетизации

- Бесплатные пользователи — показ рекламы
- Пользователи с подпиской — без рекламы
- Планируемые рекламные форматы:
  - Interstitial реклама после генерации плана
  - Rewarded video для получения дополнительного плана
- Рекламные сети: **AdMob**, **ironSource**

---

## ✅ Итог

Данный план описывает:
- структуру аналитики приложения,
- ключевые события и метрики,
- схему атрибуции пользователей,
- основу для интеграции рекламы и подписок.

План используется как база для дальнейшей технической интеграции SDK аналитики и рекламных сетей.

---

# 📊 Analytics Event Map (Этап C)

Ниже описан план аналитики для приложения **AI Meal Planner**: какие события логируем, где именно, и какие параметры передаём.
Цель — понимать воронку “от открытия до генерации плана”, ошибки AI/сети и взаимодействие с рецептами/рекламой.

---

## ✅ Общие правила

- **Названия событий**: `snake_case`, только латиница.
- **Параметры**: небольшие, понятные, без персональных данных.
- **Один пользовательский шаг → одно событие** (не спамить логами).
- События должны совпадать по смыслу во всех системах (Firebase / AppMetrica / AppsFlyer).

---

## 👤 User Properties (один раз или при изменении)

| Property              | Тип     | Когда задаём                          | Пример |
|----------------------|---------|---------------------------------------|--------|
| goal                 | String  | при выборе цели/изменении настроек    | cut / maintain / bulk |
| meals_per_day        | Int     | при изменении настроек                | 3 |
| target_calories_mode | String  | при выборе auto/manual                | auto / manual |

---

## 🧭 Основная воронка (Core Funnel)

| Event name              | Когда срабатывает                                  | Где (пример)                              | Parameters |
|------------------------|-----------------------------------------------------|-------------------------------------------|-----------|
| app_open               | приложение открыто                                  | App start (опционально)                   | — |
| settings_open          | пользователь открыл экран настроек                  | Settings/Setup screen `onAppear`          | — |
| settings_saved         | нажал сохранить настройки                           | кнопка Save                               | goal, meals_per_day, calories_mode |
| plan_generate_tap      | нажал “Generate”                                    | кнопка Generate                           | days, meals_per_day, calories_mode |
| plan_generate_start    | старт генерации (перед запросом)                    | `PlannerViewModel.generatePlan()`         | provider, model, days |
| plan_generate_success  | план успешно получен                                | `generatePlan()` после получения days     | days, total_meals |
| plan_generate_fail     | ошибка генерации                                    | `catch` в `generatePlan()`                | stage, error_type, http_status? |
| plan_saved_to_history  | сохранение в историю                                | `saveCurrentToHistory()`                  | days |
| history_open           | открытие истории                                     | History screen `onAppear`                 | — |
| history_delete         | удаление плана из истории                            | delete action                              | count |

---

## 🍲 Рецепты (TheMealDB)

| Event name         | Когда срабатывает                       | Где                              | Parameters |
|-------------------|------------------------------------------|-----------------------------------|-----------|
| recipes_search    | старт поиска рецептов по терминам        | перед циклом `searchMeals`        | terms_count |
| recipe_open       | пользователь открыл рецепт               | Recipe details open               | recipe_id, source (themealdb/youtube/source_url?) |
| recipe_open_fail  | рецепт не загрузился / пустой ответ      | catch                             | error_type |

---

## 🌐 Сеть и ошибки (Monitoring)

| Event name         | Когда срабатывает                         | Где                       | Parameters |
|-------------------|--------------------------------------------|---------------------------|-----------|
| http_retry         | выполнен retry сетевого запроса             | в `HTTPClient`            | host, path, attempt, delay_ms |
| http_fail          | финальный фейл после всех попыток           | в `HTTPClient`            | host, path, error_type, http_status? |
| rate_limited       | получили 429                                | в обработке ответов       | provider, http_status=429 |

Рекомендация: не логировать полный URL/секреты/тело ответа.

---

## 💰 Реклама (план)

> Интеграция AdMob/ironSource будет следующим шагом. Ниже план событий, которые нужно логировать.

| Event name       | Когда срабатывает                         | Parameters |
|-----------------|--------------------------------------------|-----------|
| ad_request       | запрос рекламы                              | network, placement |
| ad_loaded        | реклама загружена                           | network, placement |
| ad_impression    | показ (impression)                          | network, placement |
| ad_click         | клик                                        | network, placement |
| ad_fail          | ошибка загрузки                             | network, placement, error_code |

---

## ✅ Что считается выполнением этапа C (по аналитике)

- Firebase Analytics подключён и **видны события в DebugView**
- Есть event map (таблицы выше)
- В коде есть 3–5 ключевых событий из воронки (generate + успех/ошибка + recipe_open)
- Параметры передаются корректно
